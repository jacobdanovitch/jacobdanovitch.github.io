#!/bin/bash

# bundle exec jekyll build --config _config.yml,_config.dev.yml --watch 
# bundle exec jekyll liveserve --watch --config _config.yml,_config.dev.yml # # --port 4000 (default) # --incremental 

docker-compose up